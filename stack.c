//#include "stack.h"

/*static int stack[MAX_STACK] ;
static int sp ;

Stack_Status_Type push (int element)
{
    if(sp== MAX_STACK)
    {
        return STACK_FULL ;
    }
    stack[sp] = element ;
    ++sp ;
    return DONE ;
}

Stack_Status_Type pop (int* element)
{

    --sp ;
    if(sp == -1)
    {
        return STACK_EMPTY ;
    }
    *element = stack[++sp] ;
    return DONE ;
}*/
