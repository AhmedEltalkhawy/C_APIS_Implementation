#include"queue.h"
static int Q [Q_SIZE] ;
int IN = 0 ;
int out = 0 ;

/*inqueue (int data)
{
    if(IN == Q_SIZE)
    {
        return Q_FULL ;
    }
    Q[IN] = data ;
    ++IN ;
    return Q_DONE ;
}

dequeue (int* p)
{
    if(IN == OUT)
    {
        return Q_EMPTY ;
    }
    *p = Q[OUT] ;
    ++OUT ;
    return
}*/
